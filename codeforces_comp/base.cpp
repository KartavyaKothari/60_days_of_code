#include<bits/stdc++.h>
using namespace std;

static int speed = [](){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    return 0;
}();

class Case{
    public:
    vector<int> arr;
    int k;
};

void solve(){

}

int main(){
    int t;
    cin>>t;

    vector<Case> cases(t);

    int a,b;
    for(int i = 0 ; i < t ; i++){

    }

    for(int i = 0 ; i < t ; i++){
        solve();
        cout<<endl;
    }

    return 0;
}